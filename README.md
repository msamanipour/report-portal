###Installtion
                
----

1. install composer

`composer install`

2. generate key

`php artisan key:generate`

3. migrate db or import db.sql file

`php artisan migrate`

4. run server

`php artisan serve`
